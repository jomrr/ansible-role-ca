---
# role: ansible-role-ca
# file: tasks/authorities.yml

- name: "CA | Manage authority"
  ansible.builtin.include_tasks: authority.yml
  loop: "{{ ca_authority_order }}"
  loop_control:
    loop_var: ca_authority_name
    label: "{{ ca_authority_name }}"
