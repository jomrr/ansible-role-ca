---
# role: ca
# file: tasks/authorities.yml

- name: "CA | Manage authority"
  ansible.builtin.include_tasks: authority.yml
  loop: "{{ ca_authorities }}"
  loop_control:
    loop_var: __ca_authority
    label: "{{ __ca_authority.name }}"
