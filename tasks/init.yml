---
# role: ansible-role-ca
# file: tasks/init.yml

- name: "CA | Create base directories"
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    owner: "{{ ca_owner }}"
    group: "{{ ca_group }}"
    mode: "{{ item.mode }}"
  loop: "{{ ca_base_directories }}"
  loop_control:
    label: "{{ item.path }}"
  tags: init-dirs

- name: "CA | Store CA private key passphrase files"
  ansible.builtin.copy:
    content: "{{ ca_privatekey_passphrases[item] }}\n"
    dest: "{{ ca_base_dir }}/private/{{ item }}-ca.pass"
    owner: "{{ ca_owner }}"
    group: "{{ ca_group }}"
    mode: "0600"
  loop: "{{ ca_authority_order }}"
  loop_control:
    label: "{{ item }}"
  no_log: "{{ ca_no_log | bool }}"
  tags: init-passphrases
