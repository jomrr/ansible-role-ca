---
# role: ca
# file: tasks/crls.yml

- name: "CA | Manage authority CRLs"
  ansible.builtin.include_tasks: crl.yml
  loop: "{{ ca_authorities }}"
  loop_control:
    loop_var: ca_authority
    label: "{{ ca_authority.name }}"
